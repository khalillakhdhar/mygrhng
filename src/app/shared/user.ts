//user(id,nom,prenom,email,mdp, grade,adresse, fonction)
export class User {
    nom!: string;
    prenom!: string;
    email!: string;
    mdp!: string;
    grade!: string;
    adresse!: string;
    fonction!: string;
}
